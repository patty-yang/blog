### 使用

1.  安装 `npm install typescript`
2.  配置 `tsconfig.json`

    ```js
    // ts 六大配置
    compilerOptions
    include
    exclude
    files
    references
    extends
    ```

3.  使用 `tsc index.ts`
